[package]
name = "nevermind-ast"
version = "0.1.0"
edition = "2021"
description = "AST definitions for Nevermind"

[dependencies]
nevermind-common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
